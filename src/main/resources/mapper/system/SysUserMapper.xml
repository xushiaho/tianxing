<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianxing.system.mapper.SysUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tianxing.system.entity.SysUser">
                <id column="user_id"      property="userId"        />
                <result column="username"      property="username"    />
                <result column="password"      property="password"    />
                <result column="phone"      property="phone"    />
                <result column="email"      property="email"    />
                <result column="avatar"      property="avatar"    />
                <result column="flag"      property="flag"    />
                <result column="token"      property="token"    />
                <result column="salt"      property="salt"    />
                <result column="create_by"      property="createBy"    />
                <result column="create_time"      property="createTime"    />
                <result column="update_by"      property="updateBy"    />
                <result column="update_time"      property="updateTime"    />
                <result column="remark"      property="remark"    />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="selectSysUserVo">
        select
        user_id, username, password, phone, email, avatar, flag, token, salt, create_by, create_time, update_by, update_time, remark from sysUser
    </sql>


    <!-- 校验用户名是否唯一 -->
    <select id="checkSysUserName" parameterType="String" resultType="int">
        select count(1) from sys_user where userName=userName
    </select>
</mapper>
