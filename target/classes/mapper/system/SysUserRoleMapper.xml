<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianxing.system.mapper.SysUserRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tianxing.system.entity.SysUserRole">
                <result column="user_id"      property="userId"    />
                <result column="role_id"      property="roleId"    />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="selectSysUserRoleVo">
        select
        user_id, role_id from sysUserRole
    </sql>


    <!-- 校验用户名是否唯一 -->
    <select id="checkSysUserRoleName" parameterType="String" resultType="int">
        select count(1) from sys_user where userName=userName
    </select>
</mapper>
